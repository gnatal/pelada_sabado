const about = {
  linkURL: '/about',
  linkLabel: 'sobre',
};

const login = {
  linkURL: '/login',
  linkLabel: 'login',
};

const home = {
  linkURL: '/',
  linkLabel: 'início',
};

const posts = {
  linkURL: '/posts',
  linkLabel: 'blog',
};

const peladas = {
  linkURL: '/peladas',
  linkLabel: 'Pelada'
}

const createPelada = {
  linkURL: '/criarPelada',
  linkLabel: 'Nova pelada'
}


const profile = {
  linkURL: '/profile',
  linkLabel: 'Perfil'
}

export default {
  peladas,
  createPelada,
  about,
  profile,
  posts,
  login,
  home,
};
